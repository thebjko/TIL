# 5.3 버전
```html
<div class="row">
  <div class="align-self-center">
    <img class="d-block mx-auto" src="#" alt="">
  </div>
</div>  
```

`align-self-center` 클래스로 `div` 요소를 그리드 시스템 `row` 안에서 위아래 가운데 정렬하고, `img` 요소에 `d-block` 클래스로 블록 처럼 만든 뒤, `mx-auto` 클래스로 시작과 끝의 마진을 맞춘다.