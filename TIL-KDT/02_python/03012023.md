2023-01-03

문자열은 암시적 타입 변환이 되지 않음(implicit typecasting)

String interpolation
```python
f'{pi:.3}'
```

제어문

파이썬은 객체로 이루어져있다.
객체를 활용하기 위해 type과 연산자
조작 방식이 정해져있다

# 제어문
## 반복문

### Range 데이터 타입
`range(n)` - 0부터 1까지  
`range(n, m)` - n부터 m-1까지  
`range(n, m, s)` - n 부터 m-1 까지, s씩  

음수 시퀀스
`range(0, -6, -1)`

---

**과정이 끝나도 연락할 수 있는 사람 5명 만들기!!!**

